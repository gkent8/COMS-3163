// function that takes array and adds up the numbers

"use strict"
let sum = 0;

function sumArray(num){
    for(let i=0; i < a.length; i++)
    {
        sum += num[i];
    }
    return sum;
}
let a = [10,27,3,4];
const result = sumArray(a);
console.log(result);


